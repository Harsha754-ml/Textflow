# SMS Dashboard

## Setup

1. Copy `.env.example` to `.env` and fill in the SMSGate credentials.
2. Install dependencies in `server` and `client` once the package managers are available.
3. Start the backend and frontend development servers.

## Scripts

- `npm run dev:server`
- `npm run dev:client`
- `npm run build`
- `npm run start`
